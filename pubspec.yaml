name: myapp
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  lottie: ^3.3.1
  animated_text_kit: ^4.2.3
  google_fonts: ^6.3.1
  provider: ^6.1.5+1
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  go_router: ^16.2.1
  audioplayers: ^6.0.0
  table_calendar: ^3.1.2
  flutter_background_service: ^5.1.0
  flutter_background_service_android: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/animations/
    - assets/images/
    - assets/sounds/

# This is a comment to trigger a restart of the Dart Analysis Server.
